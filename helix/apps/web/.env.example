VITE_API_URL=http://localhost:3000/api/v1
VITE_AZURE_AD_CLIENT_ID=
VITE_AZURE_AD_TENANT_ID=
# Only needed when SPA and API use separate Azure AD app registrations.
# If omitted, falls back to VITE_AZURE_AD_CLIENT_ID.
VITE_AZURE_AD_API_CLIENT_ID=
